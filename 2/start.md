### create a deployment named `nginx-deployment` running an `nginx` container with `3` replicas and label `app: nginx`, use image `nginx:latest`.

### [YOUTUBE](https://www.youtube.com/@TheK8sLabs?sub_confirmation=1)